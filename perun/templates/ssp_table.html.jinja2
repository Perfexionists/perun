{% macro general_comparison_container(type, section_id, records) %}

    {% if records %}
        <div class="column {{ type }}">
            {%- for (key, value, title, nested_values) in records %}
                <div class="data-row">
                    <p class="data-key">{{ key }}</p>
                    <span class="material-icons pin-icon" data-section="{{ section_id }} > {{ type }} > {{ key | striptags }}">push_pin</span>
                    <p class="data-value">{{ value }}</p>
                </div>
            {%- endfor %}
        </div>
    {% else %}
        <h3>No data provided for this section</h3>
    {% endif %}

{%- endmacro %}